#include <iostream>
using namespace std;

int main() {
    int elements;
    cout << "Compiler Check: MinGW Connected!" << endl;
    cout << "Enter number of elements for an array: ";
    cin >> elements;
    cout << "Perfect! Ready to build a structure with " << elements << " elements." << endl;
    return 0;
}